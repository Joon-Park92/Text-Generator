import tensorflow as tf
import re

class Preprocessor(object):
    # 중복된 두 번의 공백을 공백으로 바꾼다.
    # Punctuation을 모두 제거한다
    # 한글과 숫자를 제외한 나머지 문자를 제거한다.




class DataGenerater(object):

    def __init__(self, batch_size, max_len, is_training):
        self.batch_size = batch_size
        self.num_batch = int( len(data) / batch_size )
        self.max_len = max_len

    def

    def get_next(self):
        return None

    def get_init_ops(self):
        return None




